import { FC } from "react";
import { ISlider } from "./types";

const Slider: FC<ISlider> = ({
  autoPlay = false,
  autoPlayTime = 5000,
  width = "100%",
  height = "100%",
}) => {
  return <>TODO: slider</>;
};

export default Slider;
